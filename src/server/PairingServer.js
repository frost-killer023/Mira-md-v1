/**
 * PairingServer.js
 *
 * Ce fichier n'est pas utilisé par le bot principal (src/index.js).
 * Il a été nettoyé pour éviter les erreurs liées à l'import
 * d'un module manquant (PairingManager).
 *
 * Le couplage WhatsApp est géré directement dans src/core/Bot.js
 * via la méthode requestPairingCode() de Baileys.
 */

module.exports = {};
